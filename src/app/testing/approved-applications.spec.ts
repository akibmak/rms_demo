import { ApprovedApplications } from '../model/approved-applications';

describe('ApprovedApplications', () => {
  it('should create an instance', () => {
    expect(new ApprovedApplications()).toBeTruthy();
  });
});
