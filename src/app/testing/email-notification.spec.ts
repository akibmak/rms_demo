import { EmailNotification } from '../model/email-notification';

describe('EmailNotification', () => {
  it('should create an instance', () => {
    expect(new EmailNotification()).toBeTruthy();
  });
});
